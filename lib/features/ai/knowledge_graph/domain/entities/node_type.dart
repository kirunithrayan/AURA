/// Represents the type of a KnowledgeNode in the Knowledge Graph.
enum NodeType {
  /// Represents an abstract concept, keyword, or topic (e.g., "Machine Learning").
  concept,
  
  /// Represents an actual physical document in the workspace.
  document,
}
